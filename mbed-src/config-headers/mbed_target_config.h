// Just a proxy header. MBed's CMakeLists.txt will look for this file directly, so we need to tell it where to find the generated config
#include "../../../mbed-cmake-config/config-headers/mbed_target_config_generated.h"